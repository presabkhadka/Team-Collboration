import { Join } from "../components/CreateButton";

export const Home = () => {
  return (
    <div className="h-screen w-screen flex justify-center items-center bg-custom-gradient1">
      <Join />
    </div>
  );
};
